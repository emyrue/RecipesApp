<h1>Inventory</h1>
<%= link_to "Add inventory", new_inventory_path%>
<div>
<% @inventories.each do |i|%>
 <div>
  <%= i.name %>
  <%= button_to "Delete", inventory_path(i), method: :delete %>
   <%= link_to "View Inventory", inventory_path(i)%>
 </div>
<% end %>
</div>
